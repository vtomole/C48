

typedef struct constructor_cell{
  void* car; //first element
  void* cdr; //rest of the list w/ first element
}constructor_cell;

typedef struct type_list{
  struct object val;
  struct type_list *next;
}type_list;
  

//Cell constructor
constructor_cell* cons(void* car, void* cdr){

  constructor_cell* cell = malloc(sizeof(constructor_cell));
  if(cell == NULL){
    printf("Error creating a new node.\n");
    exit(0);
  }
  cell->car = car;
  cell->cdr = cdr;
  return cell;
}

constructor_cell* parse(token_list* token_list){
  
}

